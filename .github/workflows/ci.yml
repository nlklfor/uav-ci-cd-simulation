name: CI test

on: [push]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Test
        run: echo "CI pipeline works ok"
      - name: Build Docker image
        run: docker build -t uav-sim .
      - name: Run Docker container
        run: docker run uav-sim
